/* ===== 起手式資料檔 =====
   把這個檔複製蓋掉 data.js（並清空 covers/），
   然後用 node scripts/add-song.mjs "歌手" "歌名" 一首首加。
   NOTES 是唱片內頁（選填），key 對 ALBUMS 的 id；沒寫會顯示「這首還沒有內頁」。 */

const ALBUMS = [
];

const NOTES = {
};
